{% set name = "pytopocomplexity" %}
{% set version = "1.0.1" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pytopocomplexity-{{ version }}.tar.gz
  sha256: 98b046268eaa5104e6c6b4087782d65039d2e0e01ff513b607042811b25a58c1

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=45
    - wheel
    - setuptools-scm >=6.2
    - pip
  run:
    - python >={{ python_min }}
    - numpy >=1.20
    - scipy >=1.6
    - rasterio >=1.2
    - dask-core >=2021.0
    - matplotlib-base >=3.3
    - tqdm >=4.0
    - numba >=0.53
    - statsmodels >=0.12
    - gdal >=3.9
    - ipython >=8.14
    - imageio >=2.11

test:
  imports:
    - pytopocomplexity
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/GeoLarryLai/pyTopoComplexity
  summary: A package for multiscale topographic complexity analysis
  license: Apache-2.0
  license_file: LICENSE
  dev_url: https://github.com/GeoLarryLai/pyTopoComplexity

extra:
  recipe-maintainers:
    - GeoLarryLai
